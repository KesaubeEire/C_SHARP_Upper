# 第1课完成：工业上位机系统全景架构

用户已经获得了工业上位机系统的完整架构图——UI 层、业务逻辑层、数据层、通信层、驱动层的职责划分，以及面试导向的讲解框架。

**为什么重要**：这是整个学习路径的地图。用户之前只有"Modbus + 简单 WinForms"的零散认识，现在有了系统化的顶层视角。所有后续课程将在这张地图上逐个深入每个区块。

**已确认的起点**：
- C# 基础尚可，WinForms 基础使用没有问题
- Modbus RTU 概念清晰（帧结构、CRC、串口）
- Modbus TCP 理论知道但实践不足
- 当前 TEST_101 项目的架构缺口已被识别（缺少业务逻辑层、数据层、设备抽象）

**用户偏好**：中文教学、实战驱动、面试导向、术语保留英文。
