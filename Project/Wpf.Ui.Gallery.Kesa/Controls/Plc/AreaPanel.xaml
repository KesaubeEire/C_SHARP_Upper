<UserControl x:Class="Wpf.Ui.Gallery.Controls.Plc.AreaPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Wpf.Ui.Gallery.Controls.Plc"
             xmlns:models="clr-namespace:Wpf.Ui.Gallery.Models.Plc"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600"
             x:Name="root">
    <Border Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource ControlElevationBorderBrush}"
            BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
        <StackPanel>
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Ellipse Grid.Column="0" Width="12" Height="12"
                         Fill="{Binding AreaColor, ElementName=root}"
                         VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Grid.Column="1"
                           Text="{Binding AreaLabel, ElementName=root}"
                           FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           VerticalAlignment="Center"/>
                <TextBox Grid.Column="2" x:Name="addrInput"
                         Text="{Binding AddressText, ElementName=root, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center" Height="28"
                         FontSize="12" Margin="4,0"/>
                <Button Grid.Column="3" x:Name="btnRead"
                        Content="▶ 读取"
                        Click="OnReadClick"
                        Margin="4,0" Height="28" FontSize="12"/>
                <Button Grid.Column="4" x:Name="btnWriteMode"
                        Content="🔒 写模式"
                        Click="OnWriteModeClick"
                        Margin="4,0" Height="28" FontSize="12"
                        Background="Transparent"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
            </Grid>

            <Grid Margin="0,4,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="地址" FontSize="11"
                           Foreground="{DynamicResource TextFillColorDisabledBrush}"/>
                <TextBlock Grid.Column="1" Text="位 (0-7)" FontSize="11"
                           Foreground="{DynamicResource TextFillColorDisabledBrush}"/>
                <TextBlock Grid.Column="2" Text="HEX" FontSize="11"
                           Foreground="{DynamicResource TextFillColorDisabledBrush}"/>
            </Grid>

            <ItemsControl x:Name="byteList" ItemsSource="{Binding ByteRows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="models:ByteRowViewModel">
                        <local:ByteRow DataContext="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock x:Name="emptyHint" Text="输入地址后点击 &#x25B6; 读取"
                       Foreground="{DynamicResource TextFillColorDisabledBrush}"
                       FontSize="13" Margin="0,8,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
