<UserControl
    x:Class="Wpf.Ui.Gallery.Controls.Plc.AreaPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wpf.Ui.Gallery.Controls.Plc"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Wpf.Ui.Gallery.Models.Plc"
    xmlns:plc="clr-namespace:Wpf.Ui.Gallery.Controls.Plc"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="root"
    d:DesignHeight="300"
    d:DesignWidth="600"
    mc:Ignorable="d">
    <Border
        Margin="0,0,0,0"
        Padding="20"
        Background="{DynamicResource CardBackground}"
        BorderThickness="1"
        CornerRadius="8">
        <StackPanel>
            <!--  标题行：色块 + 区名 + 地址输入 + 按钮  -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Ellipse
                    Grid.Column="0"
                    Width="12"
                    Height="12"
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    Fill="{Binding AreaColor, ElementName=root}" />
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    FontSize="16"
                    FontWeight="Bold"
                    Foreground="{Binding AreaColor, ElementName=root}"
                    Text="{Binding AreaLabel, ElementName=root}" />
                <TextBox
                    x:Name="addrInput"
                    Grid.Column="2"
                    Height="34"
                    Margin="8,0"
                    VerticalAlignment="Center"
                    FontSize="13"
                    Text="{Binding AddressText, ElementName=root, UpdateSourceTrigger=PropertyChanged}" />
                <ui:Button
                    x:Name="btnRead"
                    Grid.Column="3"
                    Height="34"
                    Margin="4,0"
                    Appearance="Primary"
                    Click="OnReadClick"
                    Content="▶ 读取"
                    FontSize="13" />
                <ui:Button
                    x:Name="btnWriteMode"
                    Grid.Column="4"
                    Height="34"
                    Margin="4,0"
                    Appearance="Secondary"
                    Click="OnWriteModeClick"
                    Content="🔒 写模式"
                    FontSize="13" />
            </Grid>

            <!--  列标题  -->
            <Grid Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="64" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="64" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    FontSize="12"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="地址" />
                <TextBlock
                    Grid.Column="1"
                    FontSize="12"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="位 0 → 7" />
                <TextBlock
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    FontSize="12"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="HEX" />
                <TextBlock
                    x:Name="colStatusHeader"
                    Grid.Column="3"
                    HorizontalAlignment="Center"
                    FontSize="12"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="状态" />
            </Grid>

            <!--  字节行列表  -->
            <ItemsControl x:Name="byteList" ItemsSource="{Binding ByteRows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="models:ByteRowViewModel">
                        <local:ByteRow BitToggled="OnRowBitToggled" DataContext="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!--  空状态提示  -->
            <TextBlock
                x:Name="emptyHint"
                Margin="0,12,0,0"
                FontSize="13"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                Text="输入地址后点击 &#x25B6; 读取" />
        </StackPanel>
    </Border>
</UserControl>
