<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Gallery.Controls"
    xmlns:helpers="clr-namespace:Wpf.Ui.Gallery.Helpers"
    xmlns:syntax="http://schemas.lepo.co/wpfui/2022/xaml/syntax"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />

    <Style TargetType="{x:Type controls:ControlExample}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ControlExample}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <ui:TextBlock
                            Grid.Row="0"
                            Margin="0,0,0,10"
                            FontTypography="BodyStrong"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Text="{TemplateBinding HeaderText}"
                            Visibility="{TemplateBinding HeaderText,
                                                         Converter={StaticResource NullToVisibilityConverter}}" />

                        <Border
                            x:Name="ContentBorder"
                            Grid.Row="1"
                            Padding="16"
                            Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <ContentPresenter Content="{TemplateBinding ExampleContent}" />
                        </Border>

                        <!-- Spacer to push rounded bottom corners down when SourceCode is collapsed -->
                        <Border
                            x:Name="BottomSpacer"
                            Grid.Row="2"
                            Height="0"
                            Visibility="Collapsed" />

                        <ui:CardExpander
                            x:Name="SourceCodeExpander"
                            Grid.Row="2"
                            CornerRadius="0,0,8,8"
                            Header="Source code">
                            <StackPanel>
                                <StackPanel x:Name="XamlCodeBlock">
                                    <ui:TextBlock
                                        Margin="0,0,0,5"
                                        FontTypography="BodyStrong"
                                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                        Text="XAML" />

                                    <syntax:CodeBlock
                                        Padding="0"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        BorderThickness="0"
                                        Content="{TemplateBinding XamlCode}" />
                                </StackPanel>

                                <Border
                                    x:Name="Border"
                                    Margin="0,20"
                                    BorderThickness="1"
                                    Visibility="Visible" />

                                <StackPanel x:Name="CsharpCodeBlock">
                                    <ui:TextBlock
                                        Margin="0,0,0,5"
                                        FontTypography="BodyStrong"
                                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                        Text="C#" />

                                    <syntax:CodeBlock
                                        Padding="0"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        BorderThickness="0"
                                        Content="{TemplateBinding CsharpCode}" />
                                </StackPanel>
                            </StackPanel>
                        </ui:CardExpander>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- When source code is hidden, content border has full rounded corners -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="XamlCode" Value="{x:Null}" />
                                <Condition Property="CsharpCode" Value="{x:Null}" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="SourceCodeExpander" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>

                        <!-- When source code exists, bottom spacer pushes the CardExpander up to rest on it -->
                        <Trigger SourceName="SourceCodeExpander" Property="Visibility" Value="Visible">
                            <Setter TargetName="ContentBorder" Property="CornerRadius" Value="8,8,0,0" />
                            <Setter TargetName="ContentBorder" Property="BorderThickness" Value="1,1,1,0" />
                            <!-- Move CardExpander to row 3 so it sits UNDER the content -->
                            <Setter TargetName="SourceCodeExpander" Property="Grid.Row" Value="2" />
                        </Trigger>

                        <Trigger Property="XamlCode" Value="{x:Null}">
                            <Setter TargetName="XamlCodeBlock" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="Border" Property="Visibility" Value="Collapsed" />
                        </Trigger>

                        <Trigger Property="CsharpCode" Value="{x:Null}">
                            <Setter TargetName="CsharpCodeBlock" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="Border" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
