<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <ControlTemplate x:Key="LeftCompactNavigationViewItemTemplate"
                     TargetType="{x:Type ui:NavigationViewItem}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border x:Name="MainBorder" Grid.Row="0" MinWidth="40" MinHeight="40"
                    Margin="0" Padding="0"
                    Background="{DynamicResource NavigationViewItemBackground}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{DynamicResource NavigationViewItemThickness}"
                    CornerRadius="{TemplateBinding Border.CornerRadius}">
                <Grid Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <ContentPresenter x:Name="IconContentPresenter" Margin="-1,0,0,0"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Content="{TemplateBinding Icon}" Focusable="False"
                            TextElement.FontSize="16"
                            TextElement.Foreground="{DynamicResource NavigationViewItemForeground}" />
                    </Grid>
                    <Rectangle x:Name="ActiveRectangle" Grid.Column="0" Width="3" Height="16"
                        Margin="0" HorizontalAlignment="Left" VerticalAlignment="Center"
                        Fill="{DynamicResource NavigationViewSelectionIndicatorForeground}"
                        Opacity="0.0" RadiusX="2" RadiusY="2" />
                    <ContentPresenter x:Name="ElementContentPresenter" Grid.Column="1"
                        HorizontalAlignment="Left" VerticalAlignment="Center"
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        RecognizesAccessKey="True" TextElement.FontSize="14"
                        TextElement.Foreground="{TemplateBinding Foreground}" />
                    <ContentPresenter x:Name="PART_InfoBadge" Grid.Column="2" Margin="0"
                        Content="{TemplateBinding InfoBadge}" />
                    <Grid x:Name="PART_ChevronGrid" Grid.Column="3" Width="0" Visibility="Collapsed">
                        <ui:SymbolIcon x:Name="ChevronIcon" Margin="0"
                            HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12"
                            Foreground="{DynamicResource NavigationViewItemForeground}"
                            RenderTransformOrigin="0.5, 0.5" Symbol="ChevronDown24">
                            <ui:SymbolIcon.RenderTransform>
                                <RotateTransform Angle="0" />
                            </ui:SymbolIcon.RenderTransform>
                        </ui:SymbolIcon>
                    </Grid>
                </Grid>
            </Border>

            <ItemsControl x:Name="MenuItemsPresenter" Grid.Row="1"
                AutomationProperties.AutomationId="NavigationItemItems" Focusable="False"
                ItemsSource="{TemplateBinding MenuItems}"
                KeyboardNavigation.DirectionalNavigation="Contained"
                Opacity="0.0"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Disabled"
                Visibility="Collapsed">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Margin="0" IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.Resources>
                    <!-- Level-2 items: Each gets its own MenuItemsPresenter for Level-3 -->
                    <Style TargetType="{x:Type ui:NavigationViewItem}">
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Cursor" Value="Hand" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Setter Property="IsTabStop" Value="True" />
                        <Setter Property="Focusable" Value="True" />
                        <Setter Property="SnapsToDevicePixels" Value="True" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ui:NavigationViewItem}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Border x:Name="L2Border" Grid.Row="0" Height="40"
                                            Background="Transparent" HorizontalAlignment="Stretch"
                                            BorderThickness="{DynamicResource NavigationViewItemThickness}"
                                            CornerRadius="4">
                                            <Grid Margin="30,0,0,0" HorizontalAlignment="Stretch">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" MinWidth="180" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Rectangle x:Name="L2Active" Grid.Column="0"
                                                    Width="3" Height="16" Margin="0"
                                                    HorizontalAlignment="Left" VerticalAlignment="Center"
                                                    Fill="{DynamicResource NavigationViewSelectionIndicatorForeground}"
                                                    Opacity="0.0" RadiusX="2" RadiusY="2" />
                                                <ContentPresenter x:Name="L2Icon" Grid.Column="1"
                                                    Margin="6,0,3,0" HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Content="{TemplateBinding Icon}" Focusable="False"
                                                    TextElement.FontSize="18"
                                                    TextElement.Foreground="{DynamicResource NavigationViewItemForeground}"
                                                    Visibility="{TemplateBinding Icon,
                                                        Converter={StaticResource NullToVisibilityConverter}}" />
                                                <ContentPresenter x:Name="L2Content" Grid.Column="2"
                                                    Margin="10,0,0,0" HorizontalAlignment="Stretch"
                                                    VerticalAlignment="Center"
                                                    Content="{TemplateBinding Content}"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    RecognizesAccessKey="True" TextElement.FontSize="14"
                                                    TextElement.Foreground="{TemplateBinding Foreground}" />
                                                <ContentPresenter Grid.Column="3"
                                                    Content="{TemplateBinding InfoBadge}" />
                                                <Grid x:Name="L2Chevron" Grid.Column="3"
                                                    Width="0" Visibility="Collapsed">
                                                    <ui:SymbolIcon x:Name="L2ChevronIcon" Margin="0"
                                                        HorizontalAlignment="Center" VerticalAlignment="Center"
                                                        FontSize="12"
                                                        Foreground="{DynamicResource NavigationViewItemForeground}"
                                                        RenderTransformOrigin="0.5, 0.5" Symbol="ChevronDown24">
                                                        <ui:SymbolIcon.RenderTransform>
                                                            <RotateTransform Angle="0" />
                                                        </ui:SymbolIcon.RenderTransform>
                                                    </ui:SymbolIcon>
                                                </Grid>
                                            </Grid>
                                        </Border>
                                        <ItemsControl x:Name="L2Presenter" Grid.Row="1"
                                            AutomationProperties.AutomationId="NavigationItemItems"
                                            Focusable="False"
                                            ItemsSource="{TemplateBinding MenuItems}"
                                            KeyboardNavigation.DirectionalNavigation="Contained"
                                            Opacity="0.0"
                                            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                            ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                            Visibility="Collapsed">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Margin="0" IsItemsHost="True" />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.Resources>
                                                <!-- Level-3 items: plain leaf style -->
                                                <Style TargetType="{x:Type ui:NavigationViewItem}">
                                                    <Setter Property="Margin" Value="0" />
                                                    <Setter Property="Padding" Value="0" />
                                                    <Setter Property="Cursor" Value="Hand" />
                                                    <Setter Property="VerticalAlignment" Value="Center" />
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                                    <Setter Property="IsTabStop" Value="True" />
                                                    <Setter Property="Focusable" Value="True" />
                                                    <Setter Property="SnapsToDevicePixels" Value="True" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ui:NavigationViewItem}">
                                                                <Border x:Name="L3Border" Height="40"
                                                                    Background="Transparent"
                                                                    HorizontalAlignment="Stretch"
                                                                    BorderThickness="{DynamicResource NavigationViewItemThickness}"
                                                                    CornerRadius="4">
                                                                    <Grid Margin="50,0,0,0" HorizontalAlignment="Stretch">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="Auto" />
                                                                            <ColumnDefinition Width="*" MinWidth="160" />
                                                                            <ColumnDefinition Width="Auto" />
                                                                        </Grid.ColumnDefinitions>
                                                                        <Rectangle x:Name="L3Active" Grid.Column="0"
                                                                            Width="3" Height="16"
                                                                            HorizontalAlignment="Left" VerticalAlignment="Center"
                                                                            Fill="{DynamicResource NavigationViewSelectionIndicatorForeground}"
                                                                            Opacity="0.0" RadiusX="2" RadiusY="2" />
                                                                        <ContentPresenter Grid.Column="1" Margin="12,0,0,0"
                                                                            HorizontalAlignment="Stretch" VerticalAlignment="Center"
                                                                            Content="{TemplateBinding Content}"
                                                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                            RecognizesAccessKey="True"
                                                                            TextElement.FontSize="14"
                                                                            TextElement.Foreground="{TemplateBinding Foreground}" />
                                                                    </Grid>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsActive" Value="True">
                                                                        <Setter TargetName="L3Active" Property="Opacity" Value="1.0" />
                                                                        <Setter TargetName="L3Border" Property="Background" Value="{DynamicResource NavigationViewItemBackgroundSelected}" />
                                                                    </Trigger>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="L3Border" Property="Background" Value="{DynamicResource NavigationViewItemBackgroundPointerOver}" />
                                                                    </Trigger>
                                                                    <Trigger Property="IsPressed" Value="True">
                                                                        <Setter TargetName="L3Border" Property="Background" Value="{DynamicResource NavigationViewItemBackgroundPressed}" />
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ItemsControl.Resources>
                                        </ItemsControl>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsActive" Value="True">
                                            <Setter TargetName="L2Active" Property="Opacity" Value="1.0" />
                                            <Setter TargetName="L2Border" Property="Background" Value="{DynamicResource NavigationViewItemBackgroundSelected}" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="L2Border" Property="Background" Value="{DynamicResource NavigationViewItemBackgroundPointerOver}" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="L2Border" Property="Background" Value="{DynamicResource NavigationViewItemBackgroundPressed}" />
                                        </Trigger>
                                        <Trigger Property="HasMenuItems" Value="True">
                                            <Setter TargetName="L2Chevron" Property="Visibility" Value="Visible" />
                                            <Setter TargetName="L2Chevron" Property="Width" Value="40" />
                                        </Trigger>
                                        <Trigger Property="IsExpanded" Value="True">
                                            <Setter TargetName="L2Presenter" Property="Visibility" Value="Visible" />
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="L2Presenter"
                                                            Storyboard.TargetProperty="(ItemsControl.Opacity)"
                                                            From="0.0" To="1.0" Duration="00:00:00.167" />
                                                        <DoubleAnimation Storyboard.TargetName="L2ChevronIcon"
                                                            Storyboard.TargetProperty="(Control.RenderTransform).(RotateTransform.Angle)"
                                                            To="-180" Duration="00:00:00.167" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="L2Presenter"
                                                            Storyboard.TargetProperty="(ItemsControl.Opacity)"
                                                            From="1.0" To="0.0" Duration="00:00:00.167" />
                                                        <DoubleAnimation Storyboard.TargetName="L2ChevronIcon"
                                                            Storyboard.TargetProperty="(Control.RenderTransform).(RotateTransform.Angle)"
                                                            To="0" Duration="00:00:00.167" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ItemsControl.Resources>
            </ItemsControl>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsPaneOpen" Value="True">
                <Setter TargetName="PART_InfoBadge" Property="Grid.Column" Value="2" />
            </Trigger>
            <Trigger Property="IsPaneOpen" Value="False">
                <Setter TargetName="PART_InfoBadge" Property="Grid.Column" Value="0" />
                <Setter TargetName="PART_InfoBadge" Property="VerticalAlignment" Value="Top" />
                <Setter TargetName="PART_InfoBadge" Property="HorizontalAlignment" Value="Right" />
            </Trigger>
            <Trigger Property="InfoBadge" Value="{x:Null}">
                <Setter TargetName="PART_InfoBadge" Property="Margin" Value="0" />
                <Setter TargetName="PART_InfoBadge" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="HasMenuItems" Value="True">
                <Setter TargetName="PART_ChevronGrid" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_ChevronGrid" Property="Width" Value="40" />
            </Trigger>
            <Trigger Property="IsExpanded" Value="True">
                <Setter TargetName="MenuItemsPresenter" Property="Visibility" Value="Visible" />
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="MenuItemsPresenter"
                                Storyboard.TargetProperty="(ItemsControl.Opacity)"
                                From="0.0" To="1.0" Duration="00:00:00.167" />
                            <DoubleAnimation Storyboard.TargetName="ChevronIcon"
                                Storyboard.TargetProperty="(Control.RenderTransform).(RotateTransform.Angle)"
                                To="-180" Duration="00:00:00.167" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="MenuItemsPresenter"
                                Storyboard.TargetProperty="(ItemsControl.Opacity)"
                                From="1.0" To="0.0" Duration="00:00:00.167" />
                            <DoubleAnimation Storyboard.TargetName="ChevronIcon"
                                Storyboard.TargetProperty="(Control.RenderTransform).(RotateTransform.Angle)"
                                To="0" Duration="00:00:00.167" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="IsActive" Value="True">
                <Setter TargetName="ActiveRectangle" Property="Opacity" Value="1.0" />
                <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource NavigationViewItemBackgroundSelected}" />
            </Trigger>
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter TargetName="IconContentPresenter" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="IsActive" Value="False" />
                </MultiTrigger.Conditions>
                <MultiTrigger.Setters>
                    <Setter TargetName="IconContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource NavigationViewItemForegroundPointerOver}" />
                    <Setter TargetName="ElementContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource NavigationViewItemForegroundPointerOver}" />
                </MultiTrigger.Setters>
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>
