<Page x:Class="Wpf.Ui.Gallery.Views.Pages.Plc.PollingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:Wpf.Ui.Gallery.Controls"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Title="轮询配置"
      mc:Ignorable="d">
    <StackPanel Margin="0,0,0,24">
        <ui:TextBlock Margin="0,0,0,12" FontTypography="Title" Text="轮询配置" />

        <controls:ControlExample Margin="0" HeaderText="轮询参数与控制">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="间隔 (ms)" VerticalAlignment="Center" />
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="intervalInput" Text="500" Margin="0,4" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="延迟" VerticalAlignment="Center" />
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="latencyText" Text="--" Margin="0,4" />
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <ui:Button x:Name="btnStart" Content="▶ 启动轮询" Appearance="Primary" Click="OnStart" Margin="0,0,8,0" />
                        <ui:Button x:Name="btnStop" Content="■ 停止轮询" Appearance="Secondary" Click="OnStop" IsEnabled="False" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </controls:ControlExample>

        <Border x:Name="statusBar" Background="{DynamicResource CardBackground}" CornerRadius="6" Padding="12" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal">
                <Ellipse x:Name="statusIndicator" Width="12" Height="12" Fill="{DynamicResource TextFillColorDisabledBrush}" VerticalAlignment="Center" />
                <TextBlock x:Name="statusText" Text="就绪" VerticalAlignment="Center" Margin="8,0,0,0" />
            </StackPanel>
        </Border>
    </StackPanel>
</Page>
