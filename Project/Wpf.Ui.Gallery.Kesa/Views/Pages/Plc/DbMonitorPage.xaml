<Page x:Class="Wpf.Ui.Gallery.Views.Pages.Plc.DbMonitorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:Wpf.Ui.Gallery.Controls"
      controls:PageControlDocumentation.Show="False"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:Wpf.Ui.Gallery.Views.Pages.Plc"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Title="DB 块监控">
 <ScrollViewer>
  <StackPanel Margin="24">
   <ui:TextBlock FontTypography="Title" Text="DB 块变量监控" />
   <Separator />

   <Border Background="{DynamicResource CardBackground}" CornerRadius="8" Padding="16" Margin="0,0,0,12">
    <StackPanel>
     <Grid>
      <Grid.ColumnDefinitions>
       <ColumnDefinition Width="Auto" />
       <ColumnDefinition Width="120" />
       <ColumnDefinition Width="*" />
       <ColumnDefinition Width="Auto" />
       <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <ui:Button Grid.Column="0" Content="📂 导入 .db" Click="OnImportDb" Margin="0,0,8,0" />
      <TextBlock Grid.Column="1" Text="DB 号:" VerticalAlignment="Center" Margin="8,0" />
      <TextBox Grid.Column="2" x:Name="dbNumberInput" Text="1" VerticalAlignment="Center" />
      <ui:Button Grid.Column="3" Content="📡 读取 DB" Click="OnReadDb" Margin="8,0" />
      <ui:Button Grid.Column="4" Content="✕ 清除" Appearance="Secondary" Click="OnClearDb" />
     </Grid>
     <TextBlock x:Name="dbInfoText" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" />
    </StackPanel>
   </Border>

   <Border Background="{DynamicResource CardBackground}" CornerRadius="8" Padding="16" Margin="0,0,0,12">
    <Grid>
     <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
     </Grid.RowDefinitions>
     <Grid Grid.Row="0" Margin="0,0,0,8">
      <Grid.ColumnDefinitions>
       <ColumnDefinition Width="60" />
       <ColumnDefinition Width="*" />
       <ColumnDefinition Width="80" />
       <ColumnDefinition Width="50" />
       <ColumnDefinition Width="120" />
       <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0" Text="偏移" FontWeight="Bold" FontSize="12" />
      <TextBlock Grid.Column="1" Text="变量名" FontWeight="Bold" FontSize="12" />
      <TextBlock Grid.Column="2" Text="类型" FontWeight="Bold" FontSize="12" />
      <TextBlock Grid.Column="3" Text="大小" FontWeight="Bold" FontSize="12" />
      <TextBlock Grid.Column="4" Text="值" FontWeight="Bold" FontSize="12" />
      <TextBlock Grid.Column="5" Text="注释" FontWeight="Bold" FontSize="12" />
     </Grid>
     <ItemsControl Grid.Row="1" x:Name="variableList">
      <ItemsControl.ItemTemplate>
       <DataTemplate>
        <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="0,0,0,1" Padding="4">
         <Grid>
          <Grid.ColumnDefinitions>
           <ColumnDefinition Width="60" />
           <ColumnDefinition Width="*" />
           <ColumnDefinition Width="80" />
           <ColumnDefinition Width="50" />
           <ColumnDefinition Width="120" />
           <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{Binding OffsetDisplay}" FontSize="12" />
          <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="12" FontWeight="Medium" />
          <TextBlock Grid.Column="2" Text="{Binding DataType}" FontSize="12" />
          <TextBlock Grid.Column="3" Text="{Binding Size}" FontSize="12" />
          <TextBlock Grid.Column="4" Text="{Binding Value}" FontSize="13" FontWeight="Bold"
                                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
          <TextBlock Grid.Column="5" Text="{Binding Comment}" FontSize="11"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
         </Grid>
        </Border>
       </DataTemplate>
      </ItemsControl.ItemTemplate>
     </ItemsControl>
    </Grid>
   </Border>

   <Border x:Name="statusBar" Background="{DynamicResource CardBackground}" CornerRadius="6" Padding="12">
    <StackPanel Orientation="Horizontal">
     <Ellipse x:Name="connIndicator" Width="10" Height="10" Fill="#666" VerticalAlignment="Center" />
     <TextBlock x:Name="statusText" Text="就绪" VerticalAlignment="Center" FontSize="12" />
    </StackPanel>
   </Border>
  </StackPanel>
 </ScrollViewer>
</Page>
