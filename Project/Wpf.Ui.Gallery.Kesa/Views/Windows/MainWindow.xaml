<ui:FluentWindow
    x:Class="Wpf.Ui.Gallery.Views.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Gallery.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i18n="http://schemas.lepo.co/i18n/2022/xaml"
    xmlns:local="clr-namespace:Wpf.Ui.Gallery.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Wpf.Ui.Gallery.Models.Navigation"
    xmlns:tray="http://schemas.lepo.co/wpfui/2022/xaml/tray"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{Binding ViewModel.ApplicationTitle, Mode=OneWay}"
    Width="1450"
    Height="802"
    MinWidth="900"
    d:DataContext="{d:DesignInstance local:MainWindow,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="650"
    d:DesignWidth="1000"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    SizeChanged="MainWindow_OnSizeChanged"
    WindowBackdropType="Mica"
    WindowCornerPreference="Default"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <ui:FluentWindow.Resources>
        <DataTemplate x:Key="LeafSidebarTemplate" DataType="{x:Type models:SidebarEntry}">
            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                <ContentControl Width="20" Height="20" Content="{Binding Icon}"
                    Foreground="{DynamicResource TreeViewItemForeground}" />
                <ui:TextBlock Margin="12,0,0,0" VerticalAlignment="Center"
                    Text="{Binding Label}" FontTypography="Body" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="SeparatorSidebarTemplate" DataType="{x:Type models:SidebarEntry}">
            <Rectangle Height="1" Margin="16,4,16,4" Fill="{DynamicResource ControlElevationBorderBrush}" />
        </DataTemplate>
        <DataTemplate x:Key="CustomContentSidebarTemplate" DataType="{x:Type models:SidebarEntry}">
            <ContentControl Content="{Binding CustomContent}" />
        </DataTemplate>
        <DataTemplate x:Key="DefaultSidebarTemplate" DataType="{x:Type models:SidebarEntry}">
            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                <ContentControl Width="20" Height="20" Content="{Binding Icon}"
                    Foreground="{DynamicResource TreeViewItemForeground}" />
                <ui:TextBlock Margin="12,0,0,0" VerticalAlignment="Center"
                    Text="{Binding Label}" FontTypography="Body" />
            </StackPanel>
        </DataTemplate>
        <controls:SidebarTemplateSelector x:Key="SidebarTemplateSelector"
            LeafTemplate="{StaticResource LeafSidebarTemplate}"
            SeparatorTemplate="{StaticResource SeparatorSidebarTemplate}"
            CustomContentTemplate="{StaticResource CustomContentSidebarTemplate}"
            DefaultTemplate="{StaticResource DefaultSidebarTemplate}" />
    </ui:FluentWindow.Resources>

    <ui:FluentWindow.InputBindings>
        <KeyBinding Key="F" Command="{Binding ElementName=AutoSuggestBox, Path=FocusCommand}" Modifiers="Control" />
    </ui:FluentWindow.InputBindings>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="SidebarColumn" Width="310" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ═══ 左侧 TreeView 侧栏 ═══ -->
        <Grid Grid.Column="0" Background="{DynamicResource ApplicationBackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TreeView x:Name="SidebarTreeView" Grid.Row="1" Margin="0,8,0,0"
                Background="Transparent" BorderThickness="0"
                ItemsSource="{Binding ViewModel.MenuItems}"
                SelectedItemChanged="OnSidebarTreeViewSelectedItemChanged">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type models:SidebarEntry}" ItemsSource="{Binding Children}">
                        <ContentControl Content="{Binding}" ContentTemplateSelector="{StaticResource SidebarTemplateSelector}" />
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
            <TreeView x:Name="FooterTreeView" Grid.Row="2" Margin="0,0,0,8"
                Background="Transparent" BorderThickness="0"
                ItemsSource="{Binding ViewModel.FooterMenuItems}"
                SelectedItemChanged="OnSidebarTreeViewSelectedItemChanged">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type models:SidebarEntry}" ItemsSource="{Binding Children}">
                        <ContentControl Content="{Binding}" ContentTemplateSelector="{StaticResource SidebarTemplateSelector}" />
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
        </Grid>

        <!-- ═══ NavigationView（隐藏 pane，只做内容容器）═══ -->
        <ui:NavigationView x:Name="NavigationView" Grid.Column="1"
            Padding="42,0,42,0"
            BreadcrumbBar="{Binding ElementName=BreadcrumbBar}"
            EnableDebugMessages="True"
            FrameMargin="0"
            IsBackButtonVisible="Visible"
            IsPaneOpen="False"
            IsPaneToggleVisible="False"
            IsPaneVisible="False"
            SelectionChanged="OnNavigationSelectionChanged"
            TitleBar="{Binding ElementName=TitleBar, Mode=OneWay}"
            Transition="FadeInWithSlide">
            <ui:NavigationView.Header>
                <StackPanel Margin="42,32,42,20">
                    <ui:BreadcrumbBar x:Name="BreadcrumbBar" />
                    <controls:PageControlDocumentation Margin="0,10,0,0" NavigationView="{Binding ElementName=NavigationView}" />
                </StackPanel>
            </ui:NavigationView.Header>
            <ui:NavigationView.AutoSuggestBox>
                <ui:AutoSuggestBox x:Name="AutoSuggestBox"
                    AutomationProperties.AutomationId="NavigationAutoSuggestBox"
                    PlaceholderText="{i18n:StringLocalizer 'Search'}">
                    <ui:AutoSuggestBox.Icon>
                        <ui:IconSourceElement><ui:SymbolIconSource Symbol="Search24" /></ui:IconSourceElement>
                    </ui:AutoSuggestBox.Icon>
                </ui:AutoSuggestBox>
            </ui:NavigationView.AutoSuggestBox>
            <ui:NavigationView.ContentOverlay>
                <Grid><ui:SnackbarPresenter x:Name="SnackbarPresenter" /></Grid>
            </ui:NavigationView.ContentOverlay>
        </ui:NavigationView>

        <!-- ═══ 顶层覆盖元素（TitleBar 跨列覆盖全宽）═══ -->
        <ui:TitleBar x:Name="TitleBar" Grid.ColumnSpan="2"
            Title="{Binding ViewModel.ApplicationTitle}"
            CloseWindowByDoubleClickOnIcon="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/wpfui.png" />
            </ui:TitleBar.Icon>
            <ui:TitleBar.ChromeContent>
                <ui:Button x:Name="ThemeToggleButton" Width="44" Height="30"
                    Background="Transparent" BorderThickness="0"
                    Click="OnThemeToggle" Cursor="Hand" FontSize="16" Focusable="False"
                    Icon="{ui:SymbolIcon WeatherSunny24}" ToolTip="切换深浅主题" />
            </ui:TitleBar.ChromeContent>
        </ui:TitleBar>

        <tray:NotifyIcon Grid.ColumnSpan="2"
            FocusOnLeftClick="True" Icon="pack://application:,,,/Assets/wpfui.png"
            MenuOnRightClick="True" TooltipText="WPF UI Gallery">
            <tray:NotifyIcon.Menu>
                <ContextMenu DataContext="{Binding DataContext, Source={x:Reference NavigationView}}"
                    ItemsSource="{Binding ViewModel.TrayMenuItems, Mode=OneWay}" />
            </tray:NotifyIcon.Menu>
        </tray:NotifyIcon>

        <ui:ContentDialogHost x:Name="RootContentDialog" Grid.ColumnSpan="2" />
    </Grid>
</ui:FluentWindow>
