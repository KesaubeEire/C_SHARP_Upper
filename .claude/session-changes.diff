diff --git a/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/AreaPanel.xaml b/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/AreaPanel.xaml
index 7a4f698..28a7b3c 100644
--- a/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/AreaPanel.xaml
+++ b/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/AreaPanel.xaml
@@ -5,13 +5,15 @@
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:local="clr-namespace:Wpf.Ui.Gallery.Controls.Plc"
              xmlns:models="clr-namespace:Wpf.Ui.Gallery.Models.Plc"
+             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
              mc:Ignorable="d"
              d:DesignHeight="300" d:DesignWidth="600"
              x:Name="root">
     <Border Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource ControlElevationBorderBrush}"
-            BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
+            BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,12">
         <StackPanel>
-            <Grid Margin="0,0,0,8">
+            <!-- 标题行：色块 + 区名 + 地址输入 + 按钮 -->
+            <Grid Margin="0,0,0,12">
                 <Grid.ColumnDefinitions>
                     <ColumnDefinition Width="Auto"/>
                     <ColumnDefinition Width="*"/>
@@ -29,35 +31,37 @@
                            VerticalAlignment="Center"/>
                 <TextBox Grid.Column="2" x:Name="addrInput"
                          Text="{Binding AddressText, ElementName=root, UpdateSourceTrigger=PropertyChanged}"
-                         VerticalAlignment="Center" Height="28"
-                         FontSize="12" Margin="4,0"/>
-                <Button Grid.Column="3" x:Name="btnRead"
-                        Content="▶ 读取"
-                        Click="OnReadClick"
-                        Margin="4,0" Height="28" FontSize="12"/>
-                <Button Grid.Column="4" x:Name="btnWriteMode"
-                        Content="🔒 写模式"
-                        Click="OnWriteModeClick"
-                        Margin="4,0" Height="28" FontSize="12"
-                        Background="Transparent"
-                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
+                         VerticalAlignment="Center" Height="30"
+                         FontSize="13" Margin="8,0"/>
+                <ui:Button Grid.Column="3" x:Name="btnRead"
+                           Content="▶ 读取"
+                           Click="OnReadClick"
+                           Margin="4,0" Height="30" FontSize="13"
+                           Appearance="Primary"/>
+                <ui:Button Grid.Column="4" x:Name="btnWriteMode"
+                           Content="🔒 写模式"
+                           Click="OnWriteModeClick"
+                           Margin="4,0" Height="30" FontSize="13"
+                           Appearance="Secondary"/>
             </Grid>
 
-            <Grid Margin="0,4,0,4">
+            <!-- 列标题 -->
+            <Grid Margin="0,0,0,4">
                 <Grid.ColumnDefinitions>
-                    <ColumnDefinition Width="60"/>
+                    <ColumnDefinition Width="64"/>
                     <ColumnDefinition Width="Auto"/>
-                    <ColumnDefinition Width="60"/>
-                    <ColumnDefinition Width="70"/>
+                    <ColumnDefinition Width="64"/>
+                    <ColumnDefinition Width="80"/>
                 </Grid.ColumnDefinitions>
-                <TextBlock Grid.Column="0" Text="地址" FontSize="11"
+                <TextBlock Grid.Column="0" Text="地址" FontSize="12"
                            Foreground="{DynamicResource TextFillColorDisabledBrush}"/>
-                <TextBlock Grid.Column="1" Text="位 (0-7)" FontSize="11"
+                <TextBlock Grid.Column="1" Text="位 (0-7)" FontSize="12"
                            Foreground="{DynamicResource TextFillColorDisabledBrush}"/>
-                <TextBlock Grid.Column="2" Text="HEX" FontSize="11"
+                <TextBlock Grid.Column="2" Text="HEX" FontSize="12"
                            Foreground="{DynamicResource TextFillColorDisabledBrush}"/>
             </Grid>
 
+            <!-- 字节行列表 -->
             <ItemsControl x:Name="byteList" ItemsSource="{Binding ByteRows}">
                 <ItemsControl.ItemTemplate>
                     <DataTemplate DataType="models:ByteRowViewModel">
@@ -66,9 +70,10 @@
                 </ItemsControl.ItemTemplate>
             </ItemsControl>
 
+            <!-- 空状态提示 -->
             <TextBlock x:Name="emptyHint" Text="输入地址后点击 &#x25B6; 读取"
                        Foreground="{DynamicResource TextFillColorDisabledBrush}"
-                       FontSize="13" Margin="0,8,0,0"/>
+                       FontSize="13" Margin="0,12,0,0"/>
         </StackPanel>
     </Border>
 </UserControl>
diff --git a/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/BitBlock.xaml b/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/BitBlock.xaml
index 4f032b2..0db2e2e 100644
--- a/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/BitBlock.xaml
+++ b/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/BitBlock.xaml
@@ -4,13 +4,13 @@
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              mc:Ignorable="d"
-             d:DesignWidth="32" d:DesignHeight="28">
-    <Border Width="32" Height="28" CornerRadius="4" Margin="1"
+             d:DesignWidth="36" d:DesignHeight="32">
+    <Border Width="36" Height="32" CornerRadius="4" Margin="2"
             BorderThickness="1" BorderBrush="{DynamicResource ControlElevationBorderBrush}"
             Background="{Binding IsSet, Converter={StaticResource BgConverter}}"
             MouseDown="OnMouseDown" ToolTip="点击切换" Cursor="Hand">
         <TextBlock Text="{Binding DisplayChar}" HorizontalAlignment="Center"
                    VerticalAlignment="Center" Foreground="White"
-                   FontWeight="Bold" FontSize="14"/>
+                   FontWeight="Bold" FontSize="13"/>
     </Border>
 </UserControl>
diff --git a/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/ByteRow.xaml b/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/ByteRow.xaml
index bd42d61..8534af4 100644
--- a/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/ByteRow.xaml
+++ b/Project/Wpf.Ui.Gallery.Kesa/Controls/Plc/ByteRow.xaml
@@ -6,18 +6,20 @@
              xmlns:local="clr-namespace:Wpf.Ui.Gallery.Controls.Plc"
              xmlns:conv="clr-namespace:Wpf.Ui.Gallery.Helpers"
              mc:Ignorable="d"
-             d:DesignWidth="400" d:DesignHeight="32"
+             d:DesignWidth="400" d:DesignHeight="36"
              x:Name="root">
-    <Grid Margin="0,1">
+    <Grid MinHeight="36" Margin="0,2">
         <Grid.ColumnDefinitions>
-            <ColumnDefinition Width="60"/>
+            <ColumnDefinition Width="64"/>
             <ColumnDefinition Width="Auto"/>
-            <ColumnDefinition Width="60"/>
-            <ColumnDefinition Width="70"/>
+            <ColumnDefinition Width="64"/>
+            <ColumnDefinition Width="80"/>
         </Grid.ColumnDefinitions>
+
         <TextBlock Grid.Column="0" Text="{Binding Label}"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    VerticalAlignment="Center" FontSize="13"/>
+
         <ItemsControl Grid.Column="1" ItemsSource="{Binding Bits}"
                        VerticalAlignment="Center">
             <ItemsControl.ItemsPanel>
@@ -31,11 +33,14 @@
                 </DataTemplate>
             </ItemsControl.ItemTemplate>
         </ItemsControl>
+
         <TextBlock Grid.Column="2" Text="{Binding HexText}"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    VerticalAlignment="Center" FontSize="12" Margin="8,0,0,0"/>
+
         <TextBlock Grid.Column="3" Text="已修改"
-                   Foreground="#F39C12" FontSize="11"
+                   Foreground="{DynamicResource SystemControlForegroundAccentBrush}"
+                   FontSize="12"
                    Visibility="{Binding HasChanges, Converter={StaticResource BoolToVisibilityConverter}}"
                    VerticalAlignment="Center"/>
     </Grid>
diff --git a/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/DbMonitorPage.xaml b/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/DbMonitorPage.xaml
index 57bb613..e51c608 100644
--- a/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/DbMonitorPage.xaml
+++ b/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/DbMonitorPage.xaml
@@ -7,11 +7,11 @@
       ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
       Title="DB 块监控">
  <ScrollViewer>
-  <StackPanel Margin="16">
+  <StackPanel Margin="24">
    <ui:TextBlock FontTypography="Title" Text="DB 块变量监控" />
    <Separator />
 
-   <Border Background="{DynamicResource CardBackground}" CornerRadius="8" Padding="16">
+   <Border Background="{DynamicResource CardBackground}" CornerRadius="8" Padding="16" Margin="0,0,0,12">
     <StackPanel>
      <Grid>
       <Grid.ColumnDefinitions>
@@ -31,7 +31,7 @@
     </StackPanel>
    </Border>
 
-   <Border Background="{DynamicResource CardBackground}" CornerRadius="8" Padding="16">
+   <Border Background="{DynamicResource CardBackground}" CornerRadius="8" Padding="16" Margin="0,0,0,12">
     <Grid>
      <Grid.RowDefinitions>
       <RowDefinition Height="Auto" />
diff --git a/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/GaugeDashboardPage.xaml b/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/GaugeDashboardPage.xaml
index 09dfeb1..74a842d 100644
--- a/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/GaugeDashboardPage.xaml
+++ b/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/GaugeDashboardPage.xaml
@@ -8,7 +8,7 @@
       ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
       Title="仪表盘">
     <ScrollViewer>
-        <Grid Margin="16">
+        <Grid Margin="24">
         <Grid.ColumnDefinitions>
             <ColumnDefinition Width="*" />
             <ColumnDefinition Width="*" />
diff --git a/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/ImportPage.xaml b/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/ImportPage.xaml
index e83b1ba..58077b4 100644
--- a/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/ImportPage.xaml
+++ b/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/ImportPage.xaml
@@ -11,8 +11,8 @@
         <ui:TextBlock FontTypography="Title" Text="导入 DB 块 / UDT" />
         <Separator />
 
-        <StackPanel Orientation="Horizontal">
-            <ui:Button Content="📂 导入 .db 文件" Click="OnImportDb" />
+        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
+            <ui:Button Content="📂 导入 .db 文件" Click="OnImportDb" Margin="0,0,8,0" />
             <ui:Button Content="📂 导入 .udt 文件" Click="OnImportUdt" />
         </StackPanel>
 
@@ -27,8 +27,8 @@
                                     <ColumnDefinition Width="Auto" />
                                 </Grid.ColumnDefinitions>
                                 <TextBlock Grid.Column="0" Text="{Binding Label}" FontSize="13" VerticalAlignment="Center" />
-                                <Button Grid.Column="1" Content="✕" FontSize="11" Width="24" Height="24"
-                                        Tag="{Binding}" Click="OnDeleteDb" />
+                                <ui:Button Grid.Column="1" Content="✕" FontSize="11" Width="24" Height="24"
+                                        Tag="{Binding}" Click="OnDeleteDb" Appearance="Secondary" />
                             </Grid>
                         </Border>
                     </DataTemplate>
@@ -47,8 +47,8 @@
                                     <ColumnDefinition Width="Auto" />
                                 </Grid.ColumnDefinitions>
                                 <TextBlock Grid.Column="0" Text="{Binding UdtName}" FontSize="13" VerticalAlignment="Center" />
-                                <Button Grid.Column="1" Content="✕" FontSize="11" Width="24" Height="24"
-                                        Tag="{Binding}" Click="OnDeleteUdt" />
+                                <ui:Button Grid.Column="1" Content="✕" FontSize="11" Width="24" Height="24"
+                                        Tag="{Binding}" Click="OnDeleteUdt" Appearance="Secondary" />
                             </Grid>
                         </Border>
                     </DataTemplate>
diff --git a/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/IoMonitorPage.xaml b/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/IoMonitorPage.xaml
index da7bef8..6bfd660 100644
--- a/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/IoMonitorPage.xaml
+++ b/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/IoMonitorPage.xaml
@@ -8,7 +8,7 @@
       ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
       Title="I/Q/M 监控">
     <ScrollViewer>
-        <StackPanel Margin="16">
+        <StackPanel Margin="24">
         <ui:TextBlock FontTypography="Title" Text="I / Q / M 区监控" />
         <Separator />
         <plc:AreaPanel x:Name="iPanel" AreaType="I" IsReadOnly="True" />
diff --git a/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/TrendChartPage.xaml b/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/TrendChartPage.xaml
index 5c6e347..2cc7718 100644
--- a/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/TrendChartPage.xaml
+++ b/Project/Wpf.Ui.Gallery.Kesa/Views/Pages/Plc/TrendChartPage.xaml
@@ -8,7 +8,7 @@
       ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
       Title="趋势图">
     <ScrollViewer>
-        <Grid Margin="16">
+        <Grid Margin="24">
         <Grid.RowDefinitions>
             <RowDefinition Height="Auto" />
             <RowDefinition Height="*" />
@@ -16,9 +16,8 @@
         </Grid.RowDefinitions>
 
         <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
-            <ui:TextBlock FontTypography="Title" Text="实时趋势图" VerticalAlignment="Center" />
-            <Separator Width="2" />
-            <ComboBox x:Name="timeRangeCombo" SelectedIndex="0" Width="120">
+            <ui:TextBlock FontTypography="Title" Text="实时趋势图" VerticalAlignment="Center" Margin="0,0,12,0" />
+            <ComboBox x:Name="timeRangeCombo" SelectedIndex="0" Width="120" Margin="0,0,8,0">
                 <ComboBoxItem>1 分钟</ComboBoxItem>
                 <ComboBoxItem>5 分钟</ComboBoxItem>
                 <ComboBoxItem>30 分钟</ComboBoxItem>
diff --git a/research/trioop/package.json b/research/trioop/package.json
index 73d95e0..a5c4e12 100644
--- a/research/trioop/package.json
+++ b/research/trioop/package.json
@@ -8,7 +8,8 @@
     "dev:server": "tsx watch server/index.ts",
     "dev:client": "vite",
     "build": "vite build",
-    "start": "NODE_ENV=production tsx server/index.ts"
+    "start": "NODE_ENV=production tsx server/index.ts",
+    "launch": "start.cmd"
   },
   "dependencies": {
     "@altara/core": "^0.0.2",
diff --git a/research/trioop/pnpm-workspace.yaml b/research/trioop/pnpm-workspace.yaml
index dc8858f..a810ccd 100644
--- a/research/trioop/pnpm-workspace.yaml
+++ b/research/trioop/pnpm-workspace.yaml
@@ -1,4 +1,4 @@
 allowBuilds:
   better-sqlite3: true
-  esbuild: set this to true or false
+  esbuild: true
 ignoreTarballUrlMismatch: true
diff --git a/research/trioop/src/App.css b/research/trioop/src/App.css
index 7393ba5..89ff148 100644
--- a/research/trioop/src/App.css
+++ b/research/trioop/src/App.css
@@ -1599,6 +1599,16 @@ body {
 
 .vdb-rgl-wrapper { position: relative; min-height: 80px; }
 
+.vdb-ghost-cell {
+  position: absolute;
+  z-index: 5;
+  background: rgba(33, 150, 243, 0.08);
+  border: 2px dashed #2196f3;
+  border-radius: 8px;
+  pointer-events: none;
+  transition: all 0.08s ease;
+}
+
 .react-grid-placeholder {
   background: rgba(33,150,243,0.15) !important;
   border: 2px dashed #2196f3 !important;
diff --git a/research/trioop/src/App.tsx b/research/trioop/src/App.tsx
index a386075..3da0e96 100644
--- a/research/trioop/src/App.tsx
+++ b/research/trioop/src/App.tsx
@@ -1,6 +1,7 @@
-import { useEffect, useState, useCallback } from 'react'
+import { useEffect, useState, useCallback, useRef } from 'react'
 import { usePLCData } from './hooks/usePLCData'
 import { usePLCWrite } from './hooks/usePLCWrite'
+import { reregisterAllDBs } from './hooks/useDBMapping'
 import StatusBar from './components/StatusBar'
 import PLCGrid from './components/PLCGrid'
 import ConnectionPanel from './components/ConnectionPanel'
@@ -37,7 +38,7 @@ function rangesToBytes(ranges?: { start: number; end: number }[]): number[] {
 }
 
 export default function App() {
-  const { db, io, setIo, dbBlocks, connected, lastDataTime } = usePLCData()
+  const { db, io, setIo, dbBlocks, connected, lastDataTime, ioLatency } = usePLCData()
   const { write, states, dismissError } = usePLCWrite()
   const [config, setConfig] = useState<PLCConfig | null>(null)
   const [blocks, setBlocks] = useState<DBBlockConfig[]>([])
@@ -59,6 +60,18 @@ export default function App() {
     fetch('/api/plc/db-blocks').then(r => r.json()).then(setBlocks).catch(() => {})
   }, [])
 
+  // ─── 断线重连 → 自动重新注册 DB ──────────────────────────
+  const wasConnectedRef = useRef(false)
+  useEffect(() => {
+    if (connected && !wasConnectedRef.current) {
+      wasConnectedRef.current = true
+      reregisterAllDBs().then(({ success, fail }) => {
+        if (success > 0) console.log(`自动注册 ${success} 个 DB${fail > 0 ? `, ${fail} 个失败` : ''}`)
+      })
+    }
+    if (!connected) wasConnectedRef.current = false
+  }, [connected])
+
   const handleIoToggle = useCallback(async (area: 'q' | 'm', byteAddr: number, bit: number, value: boolean) => {
     let currentByte = 0
     // 乐观更新：先改界面，同时 capture 当前字节值用于后端写入
@@ -145,7 +158,7 @@ export default function App() {
         <ConnectionPanel />
       </div>
       <div className="app__main">
-        <StatusBar config={config} connected={connected} pointCount={pointCount} lastDataTime={lastDataTime} sidebarOpen={sidebarOpen} onToggleSidebar={() => setSidebarOpen(!sidebarOpen)} />
+        <StatusBar config={config} connected={connected} pointCount={pointCount} lastDataTime={lastDataTime} ioLatency={ioLatency} sidebarOpen={sidebarOpen} onToggleSidebar={() => setSidebarOpen(!sidebarOpen)} />
         <button className="btn btn--ghost btn--sm" style={{ position: 'fixed', bottom: 8, right: 8, zIndex: 100, fontSize: 11 }} onClick={() => setShowAltara(!showAltara)}>
           {showAltara ? '关闭演示' : '组件演示'}
         </button>
diff --git a/research/trioop/src/components/StatusBar.tsx b/research/trioop/src/components/StatusBar.tsx
index f5df3b4..7a751da 100644
--- a/research/trioop/src/components/StatusBar.tsx
+++ b/research/trioop/src/components/StatusBar.tsx
@@ -9,11 +9,12 @@ interface StatusBarProps {
   connected: boolean
   pointCount: number
   lastDataTime?: number
+  ioLatency?: number
   sidebarOpen?: boolean
   onToggleSidebar?: () => void
 }
 
-export default function StatusBar({ config, connected, pointCount, lastDataTime, sidebarOpen, onToggleSidebar }: StatusBarProps) {
+export default function StatusBar({ config, connected, pointCount, lastDataTime, ioLatency, sidebarOpen, onToggleSidebar }: StatusBarProps) {
   const writableCount = config?.variables.filter(v => v.writable).length ?? 0
   const { username, role, logout } = useAuth()
   const { theme, toggle: toggleTheme } = useTheme()
@@ -52,6 +53,10 @@ export default function StatusBar({ config, connected, pointCount, lastDataTime,
       </div>
 
       <div className="status-bar__right">
+        <span className="stat stat--latency">
+          <span className="stat__label">延迟</span>
+          <span className="stat__value">{ioLatency ?? '—'} ms</span>
+        </span>
         <span className="stat">
           <span className="stat__label">变量</span>
           <span className="stat__value">{pointCount}</span>
diff --git a/research/trioop/src/components/VisualDashboard.tsx b/research/trioop/src/components/VisualDashboard.tsx
index 4d2e3fb..7b488fb 100644
--- a/research/trioop/src/components/VisualDashboard.tsx
+++ b/research/trioop/src/components/VisualDashboard.tsx
@@ -16,7 +16,7 @@ import { AlarmAnnunciatorPanel } from './altara/industrial/components/AlarmAnnun
 import { PIDTuningPanel } from './altara/industrial/components/PIDTuningPanel'
 import { ProcessFlowDiagram } from './altara/industrial/components/ProcessFlowDiagram'
 import { useContextMenu } from './VDBContextMenu'
-import { resolveVarName, loadMapping, loadAllDBData, writePLC } from '../hooks/useDBMapping'
+import { resolveVarName, loadMapping, loadAllDBData, writePLC, reregisterAllDBs } from '../hooks/useDBMapping'
 
 
 type WidgetType = 'value' | 'lamp' | 'button' | 'gauge' | 'trend' | 'oee' | 'motor' | 'predictive' | 'alarm' | 'pid' | 'signal' | 'eventlog' | 'connectionbar' | 'process'
@@ -201,6 +201,11 @@ export default function VisualDashboard({ liveData }: { liveData?: Record<string
   const [rowH, setRowH] = useState(() => { try { return Number(localStorage.getItem(ROW_HEIGHT_KEY)) || 120 } catch { return 120 } })
   const paletteRef = useRef<HTMLDivElement>(null)
   const fileRef = useRef<HTMLInputElement>(null)
+  const [currentBreakpoint, setCurrentBreakpoint] = useState<string>('lg')
+  const [ghostCell, setGhostCell] = useState<{x: number; y: number} | null>(null)
+  const [insertMenu, setInsertMenu] = useState<{x: number; y: number; top: number; left: number} | null>(null)
+  const [resizing, setResizing] = useState(false)
+  const insertMenuRef = useRef<HTMLDivElement>(null)
 
   // ─── 撤销 / 重做 ─────────────────────────────────────
   const historyRef = useRef<{ stack: any[]; idx: number }>({ stack: [], idx: -1 })
@@ -243,6 +248,68 @@ export default function VisualDashboard({ liveData }: { liveData?: Record<string
   useEffect(() => { localStorage.setItem(ROW_HEIGHT_KEY, String(rowH)) }, [rowH])
   useEffect(() => { const el = containerRef.current; if (!el) return; const ro = new ResizeObserver(entries => { for (const e of entries) setContainerWidth(e.contentRect.width) }); ro.observe(el); return () => ro.disconnect() }, [])
 
+  // ─── 网格度量 ────────────────────────────────────────────
+  const getGridMetrics = useCallback(() => {
+    const cols = COLS[currentBreakpoint] ?? 12
+    const margin = [10, 10]
+    const colWidth = (containerWidth - margin[0] * (cols - 1) - 0 * 2) / cols
+    return { cols, colWidth, cellHeight: rowH, margin }
+  }, [containerWidth, rowH, currentBreakpoint])
+
+  const mouseToGrid = useCallback((clientX: number, clientY: number) => {
+    const container = containerRef.current?.parentElement // .vdb-rgl-wrapper
+    if (!container) return null
+    const rect = container.getBoundingClientRect()
+    const { cols, colWidth, cellHeight, margin } = getGridMetrics()
+    const mx = clientX - rect.left
+    const my = clientY - rect.top
+    const col = Math.floor(mx / (colWidth + margin[0]))
+    const row = Math.floor(my / (cellHeight + margin[1]))
+    if (col < 0 || col >= cols || row < 0) return null
+    // 检查该格是否已被占
+    const layout = data.layouts[currentBreakpoint] ?? []
+    const oc = layout.some((l: any) =>
+      l.i && col < l.x + l.w && col >= l.x && row < l.y + l.h && row >= l.y
+    )
+    return oc ? null : { x: col, y: row }
+  }, [data.layouts, currentBreakpoint, getGridMetrics])
+
+  // ─── RGL 容器事件 ────────────────────────────────────────
+  const handleRglMouseMove = useCallback((e: React.MouseEvent) => {
+    if (resizing) return // 拖拽扩展大小时不显示 ghost
+    if ((e.target as HTMLElement)?.closest?.('.vdb-widget')) { setGhostCell(null); return }
+    const pos = mouseToGrid(e.clientX, e.clientY)
+    setGhostCell(pos)
+  }, [mouseToGrid, resizing])
+
+  const handleRglMouseLeave = useCallback(() => setGhostCell(null), [])
+
+  const handleRglContextMenu = useCallback((e: React.MouseEvent) => {
+    if ((e.target as HTMLElement)?.closest?.('.vdb-widget')) return // 组件上的右键走现有逻辑
+    e.preventDefault()
+    const pos = mouseToGrid(e.clientX, e.clientY)
+    if (!pos) return
+    setInsertMenu({ ...pos, top: e.clientY, left: e.clientX })
+    setGhostCell(null)
+  }, [mouseToGrid])
+
+  const handleRglClick = useCallback((e: React.MouseEvent) => {
+    if ((e.target as HTMLElement)?.closest?.('.vdb-widget')) return
+    setInsertMenu(null)
+  }, [])
+
+  // 点击外部 / Escape 关闭 insertMenu
+  useEffect(() => {
+    if (!insertMenu) return
+    const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') setInsertMenu(null) }
+    const onDown = (e: MouseEvent) => {
+      if (insertMenuRef.current && !insertMenuRef.current.contains(e.target as Node)) setInsertMenu(null)
+    }
+    const t = setTimeout(() => document.addEventListener('mousedown', onDown), 0)
+    document.addEventListener('keydown', onKey)
+    return () => { clearTimeout(t); document.removeEventListener('mousedown', onDown); document.removeEventListener('keydown', onKey) }
+  }, [insertMenu])
+
   const addWidget = useCallback((type: WidgetType) => {
     const meta = WIDGET_META[type]; const id = `w${Date.now()}`
     const cfg: Record<string, any> = {}; for (const f of CONFIG_FIELDS[type] || []) cfg[f.key] = f.default
@@ -253,6 +320,33 @@ export default function VisualDashboard({ liveData }: { liveData?: Record<string
     setFormTitle(meta.label); setFormType(type); setFormCfg({ ...cfg }); setEditing(id); setShowPalette(false)
   }, [data.layouts])
 
+  // ─── 在指定网格位置插入组件 ──────────────────────────────
+  const addWidgetAt = useCallback((type: WidgetType, x: number, y: number) => {
+    const meta = WIDGET_META[type]; const id = `w${Date.now()}`
+    const cfg: Record<string, any> = {}; for (const f of CONFIG_FIELDS[type] ?? []) cfg[f.key] = f.default
+    const bp = currentBreakpoint
+    const layout = (data.layouts[bp] ?? []).map((l: any) => ({ ...l }))
+    const newItem = { i: id, x, y, w: 1, h: 1 }
+    const newLayout = [...layout, newItem]
+    // 碰撞检测：把和 (x,y,1x1) 重叠的组件向下推 1 格
+    for (let pass = 0; pass < 30; pass++) {
+      let moved = false
+      for (const item of newLayout) {
+        if (item.i === id) continue
+        if (item.x < x + 1 && item.x + item.w > x && item.y < y + 1 && item.y + item.h > y) {
+          item.y += 1; moved = true
+        }
+      }
+      if (!moved) break
+    }
+    setData(d => ({
+      widgets: [...d.widgets, { id, type, title: meta.label, config: cfg }],
+      layouts: { ...d.layouts, [bp]: newLayout },
+    }))
+    setGhostCell(null); setInsertMenu(null)
+    setFormTitle(meta.label); setFormType(type); setFormCfg({ ...cfg }); setEditing(id)
+  }, [data.layouts, currentBreakpoint, setData])
+
   // 点击外部 / Escape → 关闭 palette dropdown
   useEffect(() => {
     if (!showPalette) return
@@ -292,6 +386,7 @@ export default function VisualDashboard({ liveData }: { liveData?: Record<string
         <Tooltip content="导出 JSON"><button className="btn btn--ghost btn--sm" onClick={exportData}>📤</button></Tooltip>
         <input ref={fileRef} type="file" accept=".json" onChange={importData} style={{ display:'none' }} />
         <Tooltip content="导入 JSON"><button className="btn btn--ghost btn--sm" onClick={() => fileRef.current?.click()}>📥</button></Tooltip>
+        <Tooltip content="重新注册所有已导入 DB"><button className="btn btn--ghost btn--sm" onClick={() => reregisterAllDBs()}>↻</button></Tooltip>
         <div style={{ position: 'relative' }} ref={paletteRef}>
           <button className="btn btn--sm btn--primary" style={{ display: 'inline-flex', alignItems: 'center', gap: 4 }} onClick={() => setShowPalette(p => !p)}>
             <span>+ 添加</span>
@@ -320,7 +415,7 @@ export default function VisualDashboard({ liveData }: { liveData?: Record<string
       {data.widgets.length === 0 ? (
         <div className="db-empty" style={{ textAlign: 'center', padding: 40 }}>点击「+ 添加组件」开始构建仪表盘</div>
       ) : (
-        <div className="vdb-rgl-wrapper">
+        <div className="vdb-rgl-wrapper" onMouseMove={handleRglMouseMove} onMouseLeave={handleRglMouseLeave} onContextMenu={handleRglContextMenu} onClick={handleRglClick}>
           <div ref={containerRef} style={{ width: "100%" }}><Responsive width={containerWidth}
             className="vdb-rgl"
             layouts={data.layouts}
@@ -328,10 +423,13 @@ export default function VisualDashboard({ liveData }: { liveData?: Record<string
             cols={COLS}
             rowHeight={rowH}
             onLayoutChange={onLayoutChange}
+            onBreakpointChange={(bp) => setCurrentBreakpoint(bp)}
+            onResizeStart={() => setResizing(true)}
+            onResizeStop={() => setResizing(false)}
             draggableHandle=".vdb-widget__bar"
             isDraggable
             isResizable
-            compactType="vertical"
+            compactType={null}
             preventCollision={false}
             margin={[10, 10]}
             containerPadding={[0, 0]}
@@ -353,7 +451,30 @@ export default function VisualDashboard({ liveData }: { liveData?: Record<string
                 <div className="vdb-widget__body" onMouseDown={e => e.stopPropagation()}><ResizeWrapper>{renderWidget(w.type, w.config, liveData)}</ResizeWrapper></div>
               </div>
             ))}
-          </Responsive></div>
+          </Responsive>
+          {/* Ghost cell */}
+          {ghostCell && (() => {
+            const { colWidth, cellHeight, margin } = getGridMetrics()
+            return <div className="vdb-ghost-cell" style={{
+              left: ghostCell.x * (colWidth + margin[0]),
+              top: ghostCell.y * (cellHeight + margin[1]),
+              width: colWidth,
+              height: cellHeight,
+            }} />
+          })()}
+          {/* 右键插入菜单 */}
+          {insertMenu && (
+            <div ref={insertMenuRef} className="vdb-ctx" style={{ position: 'fixed', zIndex: 99999, left: insertMenu.left, top: insertMenu.top }}>
+              {Object.entries(WIDGET_META).map(([type, meta]) => (
+                <button key={type} className="vdb-ctx__item"
+                  onClick={() => addWidgetAt(type as WidgetType, insertMenu.x, insertMenu.y)}>
+                  <span className="vdb-ctx__icon">{meta.icon}</span>
+                  {meta.label}
+                </button>
+              ))}
+            </div>
+          )}
+          </div>
         </div>
       )}
 
diff --git a/research/trioop/src/hooks/useDBMapping.ts b/research/trioop/src/hooks/useDBMapping.ts
index 32d5d5e..6c8ceb0 100644
--- a/research/trioop/src/hooks/useDBMapping.ts
+++ b/research/trioop/src/hooks/useDBMapping.ts
@@ -77,6 +77,33 @@ export function clearUDTCache(): void {
   try { localStorage.removeItem(UDT_FILES_KEY) } catch {}
 }
 
+
+
+/** 重新注册所有已导入的 DB 到当前连接 */
+export async function reregisterAllDBs(): Promise<{ success: number; fail: number }> {
+  try {
+    const res = await fetch('/api/plc/imported-dbs')
+    if (!res.ok) return { success: 0, fail: 0 }
+    const dbs: { dbNumber: number; dbName: string }[] = await res.json()
+    if (dbs.length === 0) return { success: 0, fail: 0 }
+    const mapping = loadMapping()
+    let success = 0, fail = 0
+    for (const db of dbs) {
+      try {
+        const dbNumber = mapping[db.dbName] ?? db.dbNumber
+        const key = `${db.dbNumber}_${db.dbName}`
+        const r = await fetch(`/api/plc/imported-dbs/${encodeURIComponent(key)}/refresh`, {
+          method: 'POST', headers: { 'Content-Type': 'application/json' },
+          body: JSON.stringify({ dbNumber, dbName: db.dbName }),
+        })
+        const data = await r.json()
+        if (data.success) { success++ } else { fail++ }
+      } catch { fail++ }
+    }
+    return { success, fail }
+  } catch { return { success: 0, fail: 0 } }
+}
+
 /** 写入 PLC：bool 走 modifyBit(RMW), 数值直接写 */
 export async function writePLC(fullName: string, value: number): Promise<void> {
   const { dbNumber, varName } = resolveVarName(fullName)
diff --git a/research/trioop/src/hooks/usePLCData.ts b/research/trioop/src/hooks/usePLCData.ts
index 102531f..97ed79d 100644
--- a/research/trioop/src/hooks/usePLCData.ts
+++ b/research/trioop/src/hooks/usePLCData.ts
@@ -28,6 +28,8 @@ export function usePLCData() {
   const [dbBlocks, setDbBlocks] = useState<Record<string, number[] | null>>({})
   const [connected, setConnected] = useState(false)
   const [lastDataTime, setLastDataTime] = useState(0)
+  const [ioLatency, setIoLatency] = useState(0)
+  const lastIoTimeRef = useRef(0)
   const esRef = useRef<EventSource | null>(null)
   const retryRef = useRef<ReturnType<typeof setTimeout> | undefined>(undefined)
 
@@ -48,7 +50,13 @@ export function usePLCData() {
         setDb(payload.db ?? {})
         setIo({ i: payload.io?.i ?? {}, q: payload.io?.q ?? {}, m: payload.io?.m ?? {} })
         setDbBlocks(payload.dbBlocks ?? {})
-        setLastDataTime(Date.now())
+        const now = Date.now()
+        setLastDataTime(now)
+        // 跟踪 I 区更新间隔（两次信号接收时间差）
+        if (lastIoTimeRef.current > 0) {
+          setIoLatency(now - lastIoTimeRef.current)
+        }
+        lastIoTimeRef.current = now
       } catch { /* ignore malformed data */ }
     }
 
@@ -67,5 +75,5 @@ export function usePLCData() {
     }
   }, [connect])
 
-  return { db, io, setIo, dbBlocks, connected, lastDataTime }
+  return { db, io, setIo, dbBlocks, connected, lastDataTime, ioLatency }
 }
